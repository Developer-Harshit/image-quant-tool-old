let vertexShader, fragmentShader
let frameCount = 0
let camera, scene, renderer
let texture, planeMesh
let imageElement, outputImage, outputAnchor
let iSrc = './samples/sample.jpg'
let myPalette
let myData = [1]
